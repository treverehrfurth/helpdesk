# Mock mode — committed, no secrets.
# Loaded by Vite when running with --mode mock (i.e. npm run dev:mock).
# Overrides .env so the browser-only mock works regardless of local .env contents.
VITE_AUTH_MODE=mock
VITE_USE_MOCK_API=true
VITE_DEV_ROLE=admin
